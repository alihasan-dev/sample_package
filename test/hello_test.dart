import 'package:flutter_test/flutter_test.dart';
import 'package:sample_formatter/date_utils.dart';

void main() {
  test('adds one to input values', () {
    final calculator = DateFormatter();
  });
}
