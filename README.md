# 📅 sample_formatter

A lightweight and developer-friendly Flutter/Dart package that simplifies date parsing, formatting, and transformation using clean extension methods.

Built on top of the powerful `intl` package, this library helps you handle multiple date formats, convert patterns, and generate human-readable date strings with minimal and readable code.

---

## ✨ Features

- ✅ Format `DateTime` using custom patterns  
- ✅ Parse date strings into `DateTime` objects  
- ✅ Convert between different date formats  
- ✅ Human-readable relative time (e.g., `2 days ago`, `Yesterday`)  
- ✅ Extension-based clean and expressive API  
- ✅ Null-safe and production-ready  
- ✅ Lightweight and easy to integrate  

---

## 🚀 Getting Started

Add the package to your `pubspec.yaml`:

```yaml
dependencies:
  sample_formatter: ^1.0.0
